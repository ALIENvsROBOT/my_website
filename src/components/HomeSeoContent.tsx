export default function HomeSeoContent() {
  // The root layout owns the canonical Person, ProfilePage, and WebSite graph.
  // Avoid duplicate markup and a hidden FAQ so crawlers prioritize the complete,
  // visible biography and project content instead.
  return null;
}
